package org.apache.cayenne.experiment;

public class ExpressionConsumer implements PairsConsumer {
    @Override
    public <T> T consume(JoinContentVisitor<T> joinContentVisitor) {
        return null;
    }
}
